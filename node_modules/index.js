const express = require('express');
const axios = require('axios');
const app = express();
const path = require('path');

// Your webhook URL from environment variables
const WEBHOOK_URL = process.env.WEBHOOK_URL;

// Webhook sending function and all the middleware code from my previous response
// [Include the full code I provided above]
